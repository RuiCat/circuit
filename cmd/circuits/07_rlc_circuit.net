# =============================================================================
# RLC 串联振荡电路 (RLC Series Oscillator)
# =============================================================================
# 含电阻、电感、电容的串联电路，DC 阶跃激励下产生阻尼振荡。
# 根据 R 的大小，呈现过阻尼、临界阻尼或欠阻尼振荡。
#
# 电路结构:
#   V1 (5V DC 阶跃) ---+---[R1 100Ω]---+---[L1 1mH]---+---[C1 1μF]--- GND (-1)
#                      |               |               |
#                    node 1         node 2          node 3
#
# 关键参数:
#   R = 100Ω, L = 1mH, C = 1μF
#
#   谐振频率 f₀ = 1/(2π√LC) = 1/(2π√(1e-3 × 1e-6)) ≈ 5033Hz ≈ 5kHz
#   谐振角频率 ω₀ = 1/√LC ≈ 31623 rad/s
#
#   特征阻抗 Z₀ = √(L/C) = √(1e-3/1e-6) = √1000 ≈ 31.6Ω
#
#   阻尼系数 α = R/(2L) = 100/(2×1e-3) = 50000 s⁻¹
#   欠阻尼条件: α < ω₀ → 50000 > 31623 → 事实上这是过阻尼!
#
#   欠阻尼版: 若 R = 10Ω → α = 5000 < ω₀ → 衰减振荡
#   本电路 R=100Ω 产生过阻尼响应，电容电压单调上升无振荡。
#   调整 R 到较小值（如10Ω）可观察振荡。
#
# 预期结果:
#   节点 1 = 5V DC
#   节点 3 (电容电压) ≈ 从 0V 缓慢上升至 5V (过阻尼，无振荡)
#
# 运行命令:
#   circuit -t 0.002 cmd/circuits/07_rlc_circuit.net
# =============================================================================

V1 [1,-1] [0,0,0,0,5]      # DC 5V 阶跃激励
R1 [1,2] [100]               # 阻尼电阻 100Ω: 决定阻尼特性
L1 [2,3] [1e-3]              # 电感 1mH: 抵抗电流变化，与电容交换能量
C1 [3,-1] [1e-6]             # 电容 1μF: 抵抗电压变化，与电感交换能量
