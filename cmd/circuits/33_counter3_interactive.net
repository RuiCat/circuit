# =============================================================================
# 3 位异步二进制计数器 (3-bit Ripple Counter, DFF 级联二分频)
# =============================================================================
# 每个 T 触发器 T 恒接 VCC（=1），Q 输出作下一级时钟：
#   Q0(2) ← clk(1)     每 1 个时钟翻转
#   Q1(5) ← nq0(3)    每 2 个时钟翻转（Q0 下降沿触发）
#   Q2(7) ← nq1(6)    每 4 个时钟翻转（Q1 下降沿触发）
# → 计数序列 000 → 001 → 010 → 011 → 100 → 101 → 110 → 111 → 000 ...
#
# 操作 (circuit interactive cmd/circuits/33_counter3_interactive.net):
#   > step                ← 初始化
#   > step ×8             ← 连续推进 8 个时钟周期，观察 Q0/Q1/Q2 翻转
#   > show                ← node2(Q0) node5(Q1) node7(Q2)
#
# 观察：Q0 每个时钟翻转；Q1 每 2 个时钟翻转；Q2 每 4 个时钟翻转。
# 复位开关：RST=1 时把 Q0/Q1/Q2 经二极管下拉强制清零，
# 置 0 后从 000 重新计数。（复位路径见下方 D1/D2/D3）
# =============================================================================

V1 [100,-1] [0,0,0,0,5]

# 时钟源：1kHz 方波脉冲（上升沿触发 DFF）
V2 [1,-1] [5,0,1000,0,5,0.5]

# ---- 同步计数器组合逻辑（进位）----
# D0 = ¬Q0                  （Q0 每时钟翻转）
U1 [2,-1,10] [0,5]
R10 [10,-1] [10000]
# D1 = Q1 ⊕ Q0              （Q0=1 时 Q1 翻转）
U2 [2,5,11] [5,5]
R11 [11,-1] [10000]
# D2 = Q2 ⊕ (Q0·Q1)         （Q0=Q1=1 时 Q2 翻转）
U3 [2,5,12] [1,5]
R12 [12,-1] [10000]
U4 [7,12,13] [5,5]
R13 [13,-1] [10000]

# ---- 3 个 DFF（全部同步于同一时钟 V2）----
DFF1 [1,10,2,3] [5]        # Q0: clk=1, d=D0(10), q=2, nq=3
R2 [2,-1] [10000]
R3 [3,-1] [10000]
DFF2 [1,11,5,6] [5]        # Q1: clk=1, d=D1(11), q=5, nq=6
R5 [5,-1] [10000]
R6 [6,-1] [10000]
DFF3 [1,13,7,8] [5]        # Q2: clk=1, d=D2(13), q=7, nq=8
R7 [7,-1] [10000]
R8 [8,-1] [10000]
