# =============================================================================
# 两相主从触发器 (CPU 组件: msff, 逻辑实现) — 交互测试
# =============================================================================
# CPU 的 msff（两相主从触发器）= 主锁存(dlt, PHI1) + 从锁存(dlt, PHI2)：
#   PHI1=1 时主锁存采样 D → M；PHI1=0 后 PHI2=1 时从锁存输出 M → Q。
# 两相非交叠时钟避免主从竞态（CPU 时钟核心）。
#
# 操作:
#   > step
#   > set D_IN 1 ; set PHI1 1 ; step   → 主锁存采样 → M=5V（内部）
#   > set PHI1 0 ; set PHI2 1 ; step   → 从锁存输出 → Q(40)=5V
#   > set D_IN 0 ; set PHI1 1 ; set PHI2 0 ; step  → 主锁存采样 D=0
#   > set PHI1 0 ; set PHI2 1 ; step   → Q(40)=0V
# =============================================================================

V1 [100,-1] [0,0,0,0,5]

# 输入开关
B1 [100,10] [D_IN,0.5,1]
R10 [10,-1] [10000]
B2 [100,11] [PHI1,0.5,1]
R11 [11,-1] [10000]
B3 [100,12] [PHI2,0.5,1]
R12 [12,-1] [10000]

# ---- 主锁存: S1=D·PHI1, R1=¬D·PHI1 → M(30) ----
U1 [10,11,13] [1,5]
U2 [14,11,15] [1,5]
U5 [10,-1,14] [0,5]
SR1 [13,15,30,31] [5]    # M=30
R30 [30,-1] [10000]

# ---- 从锁存: S2=M·PHI2, R2=¬M·PHI2 → Q(40) ----
U3 [30,12,16] [1,5]
U4 [17,12,18] [1,5]
U6 [30,-1,17] [0,5]
SR2 [16,18,40,41] [5]    # Q=40
R40 [40,-1] [10000]
